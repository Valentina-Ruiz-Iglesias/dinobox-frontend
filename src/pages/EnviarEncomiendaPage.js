import React from 'react';
import FormEncomienda from '../components/FormEncomienda';

const EnviarEncomiendaPage = () => (
  <div>
    <h2>Enviar nueva encomienda</h2>
    <FormEncomienda />
  </div>
);

export default EnviarEncomiendaPage;
